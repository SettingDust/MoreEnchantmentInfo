{
    "schemaVersion": 1,
    "id": "${id}",
    "version": "${version}",
    "name": "${name}",
    "description": "${description}",
    "authors": [
        "${author}"
    ],
    "contact": {
        "homepage": "${source}",
        "sources": "${source}",
        "issues": "${source}/issues"
    },
    "license": "MIT",
    "icon": "assets/${id}/icon.png",
    "environment": "*",
    "entrypoints": {
        "client": [
            {
                "adapter": "kotlin",
                "value": "settingdust.more_enchantment_info.MoreEnchantmentInfoClient::clientInit"
            }
        ],
        "jei_mod_plugin": [
            {
                "adapter": "kotlin",
                "value": "settingdust.more_enchantment_info.jei.JEIMoreEnchantmentInfo"
            }
        ]
    },
    "depends": {
        "fabricloader": "${fabric_loader}",
        "minecraft": "${minecraft}",
        "fabric-language-kotlin": "${fabric_kotlin}"
    },
    "mixins": ["${id}.mixins.json"]
}
